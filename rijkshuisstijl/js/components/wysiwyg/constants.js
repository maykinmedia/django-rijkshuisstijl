import BEM from 'bem.js';

/** @const {string} Wysiwyg block name. */
export const BLOCK_WYSIWYG = 'wysiwyg';

/** @const {string} Wysiwygs. */
export const WYSIWYGS = BEM.getBEMNodes(BLOCK_WYSIWYG);
