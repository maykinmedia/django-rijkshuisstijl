import BEM from 'bem.js';

/** @const {string} Skiplink block name. */
export const BLOCK_SKIPLINK = 'skiplink';

/** @const {string} Skiplinks. */
export const SKIPLINKS = BEM.getBEMNodes(BLOCK_SKIPLINK);
