import BEM from 'bem.js';

/** @const {string} Confirm form block name. */
export const BLOCK_CONFIRM_FORM = 'confirm-form';

/** @const {string} Confirm forms. */
export const CONFIRM_FORMS = BEM.getBEMNodes(BLOCK_CONFIRM_FORM);
