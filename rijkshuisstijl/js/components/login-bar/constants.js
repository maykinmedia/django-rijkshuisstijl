import BEM from 'bem.js';

/** @const {string} Login bar block name. */
export const BLOCK_LOGIN_BAR = 'login-bar';

/** @const {string} Login bars. */
export const LOGIN_BARS = BEM.getBEMNodes(BLOCK_LOGIN_BAR);
