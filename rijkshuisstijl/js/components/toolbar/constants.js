import BEM from 'bem.js';

/** @const {string} Toolbar block name. */
export const BLOCK_TOOLBAR = 'toolbar';

/** @const {string} Toolbars. */
export const TOOLBARS = BEM.getBEMNodes(BLOCK_TOOLBAR);
