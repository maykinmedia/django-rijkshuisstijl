import BEM from 'bem.js';

/** @const {string} Title header block name. */
export const BLOCK_TITLE_HEADER = 'title-header';

/** @const {string} Title headers. */
export const TITLE_HEADERS = BEM.getBEMNodes(BLOCK_TITLE_HEADER);
