import BEM from 'bem.js';

/** @const {string} Navigation bar block name. */
export const BLOCK_NAVIGATION_BAR = 'navigation-bar';

/** @const {string} Navigation bars. */
export const NAVIGATION_BARS = BEM.getBEMNodes(BLOCK_NAVIGATION_BAR);
