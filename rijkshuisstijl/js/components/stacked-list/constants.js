import BEM from 'bem.js';

/** @const {string} Stacked list block name. */
export const BLOCK_STACKED_LIST = 'stacked-list';

/** @const {string} Stacked lists. */
export const STACKED_LISTS = BEM.getBEMNodes(BLOCK_STACKED_LIST);
