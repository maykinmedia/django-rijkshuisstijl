import BEM from 'bem.js';

/** @const {string} Textbox block name. */
export const BLOCK_TEXTBOX = 'textbox';

/** @const {string} Textboxs. */
export const TEXTBOXES = BEM.getBEMNodes(BLOCK_TEXTBOX);
