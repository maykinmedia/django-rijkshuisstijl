import BEM from 'bem.js';

/** @const {string} Key value table block name. */
export const BLOCK_KEY_VALUE_TABLE = 'key-value-table';

/** @const {string} Key value tables. */
export const KEY_VALUE_TABLES = BEM.getBEMNodes(BLOCK_KEY_VALUE_TABLE);
